# Credentials

Author with handles only, for example `${secrets.api_token}`. Enrollment is an
operator operation. At execution, the credential adapter checks the exact
approved revision, actor, and declared scope, and releases the value only to
the authorized worker boundary.
