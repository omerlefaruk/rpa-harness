# Verification and Evidence

An action result is not proof. Record expected state, observed state, verifier,
outcome, and Evidence references. Mismatch and verifier errors fail closed.
Evidence is recursively redacted and stored by content hash; a sink failure
cannot rewrite lifecycle truth.
