# Windows UI Automation

Use automation id, name, class, and tree path before weak fallbacks. Focus the
window explicitly, verify every write independently, and capture redacted
Evidence. UIA and Task Scheduler require an interactive Windows host; other
platforms must return a clear capability error.
