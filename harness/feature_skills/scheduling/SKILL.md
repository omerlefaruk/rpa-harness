# Scheduling

Schedule an immutable revision and credential handles, never mutable source or
secret values. Validate runtime version, workspace lock, credential presence,
approval expiry, and platform capability before launch. Inspect and remove
tasks through the shared application interface.
