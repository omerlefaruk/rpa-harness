# Excel

Discover the workbook and sheet through a read-only observation. Use the Excel
port for governed writes, save through the port, close the handle, and reopen a
real temporary workbook for independent Verification. An interrupted save is a
Reconciliation case, never an automatic duplicate write.
