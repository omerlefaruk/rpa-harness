# HTTP API

Use credential handles, typed request/response models, pagination bounds,
idempotency keys, and bounded retries. A mutation is followed by a read-back
Verification. Transport failure after dispatch is Ambiguous until a read-only
probe proves Applied or Not Applied.
