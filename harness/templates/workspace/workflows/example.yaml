id: product_smoke
name: Product Smoke
version: "0.1.0"
type: mixed
description: Verifies the local rpa-harness runtime without external systems.

steps:
  - id: runtime_ready
    description: Runtime is installed and workflow execution works.
    action:
      type: no_op
    success_check:
      - type: always_pass
    allow_without_success_check: false

