# ota_recent_reviews

Excel-driven recent OTA review extraction workflow.

- Config: `config.yaml`
- Project workflow descriptor: `workflows/main.yaml`

Run:

```bash
python main.py --audit-workflow projects/ota_recent_reviews/workflows/main.yaml
python main.py --run-yaml projects/ota_recent_reviews/workflows/main.yaml
```
